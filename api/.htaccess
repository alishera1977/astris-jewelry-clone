# Protect secret files from direct browser access
<FilesMatch "^(yookassa-config|keys)\.php$">
  Require all denied
</FilesMatch>

<IfModule !mod_authz_core.c>
  <FilesMatch "^(yookassa-config|keys)\.php$">
    Order allow,deny
    Deny from all
  </FilesMatch>
</IfModule>
